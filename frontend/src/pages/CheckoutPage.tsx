const CheckoutPage = () => {
  return (
    <section>
      <h1>Checkout</h1>
      <p>
        The checkout flow will be connected to the reservation endpoint and
        payment module next.
      </p>
    </section>
  );
};

export default CheckoutPage;
