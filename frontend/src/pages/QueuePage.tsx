const QueuePage = () => {
  return (
    <section>
      <h1>Waiting Room</h1>
      <p>
        High demand events will route users through a queue to preserve capacity
        and prevent overload.
      </p>
    </section>
  );
};

export default QueuePage;
